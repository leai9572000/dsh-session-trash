# dsh-session-trash bundle layer: inserts the session-trash plugin row.
# The row id stays stable so re-adds and profile patches can target it.
- insert:
    - id: session-trash
      name: dsh-session-trash
